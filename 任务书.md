# LAN Transfer 局域网文件传输工具开发任务书

## 1. 项目名称

**LAN Transfer**

Windows 局域网点对点文件传输工具。

---

## 2. 项目背景

当前使用环境中，两台 Windows PC 位于同一局域网，但 Windows 文件共享功能受到限制，无法依赖 SMB、共享文件夹、网络邻居等方式完成文件传输。

需要开发一个独立的 Windows 应用，使两台电脑在不依赖 SMB、不依赖互联网、不依赖中心服务器的情况下，通过局域网直接进行文件和文件夹传输。

本软件本质上是：

**Windows PC ↔ Windows PC 的 P2P 文件传输工具。**

禁止通过修改系统安全策略、绕过网络隔离、建立外部隧道等方式实现功能。

如果局域网本身禁止终端之间 TCP/IP 通信，软件应明确诊断并提示原因，而不是尝试规避网络策略。

---

# 3. 核心目标

软件应实现以下能力：

* 不使用 SMB
* 不使用 Windows 文件共享
* 不依赖互联网
* 不依赖云服务器
* 两台 PC 直接传输
* 自动发现局域网设备
* 支持手工 IP 连接
* 支持文件传输
* 支持文件夹传输
* 支持多文件
* 支持大文件
* 支持断点续传
* 支持 SHA-256 完整性校验
* 支持传输暂停/继续/取消
* 支持设备可信配对
* 支持接收确认
* 支持拖放发送
* 显示实时速度、进度、剩余时间
* 支持历史记录

目标文件大小：

**0 B ～ 100 GB+**

软件应采用流式传输，严禁将整个文件一次性读入内存。

---

# 4. 目标平台

## 操作系统

最低：

* Windows 10 x64

推荐：

* Windows 11 x64

第一版只需要支持：

**Windows x64**

暂不开发：

* macOS
* Linux
* Android
* iOS

---

# 5. 技术栈

使用：

```text
.NET 10 LTS
C#
WPF
ASP.NET Core Kestrel
HttpClient
System.Net.Sockets
System.Security.Cryptography
System.Text.Json
SQLite
Serilog
```

UI：

```text
WPF
MVVM
```

建议：

```text
CommunityToolkit.Mvvm
```

服务端：

```text
ASP.NET Core Kestrel
```

客户端：

```text
HttpClient
```

网络发现：

```text
UDP Broadcast
UdpClient
```

数据存储：

```text
SQLite
```

日志：

```text
Serilog
```

---

# 6. 软件总体架构

每个客户端同时承担：

```text
Client
+
Server
```

即：

```text
PC-A                               PC-B

┌──────────────────┐              ┌──────────────────┐
│ LAN Transfer     │              │ LAN Transfer     │
│                  │              │                  │
│ UDP Discovery    │◀────────────▶│ UDP Discovery    │
│                  │              │                  │
│ HTTP Client      │─────────────▶│ HTTP Server      │
│                  │              │                  │
│ HTTP Server      │◀─────────────│ HTTP Client      │
│                  │              │                  │
│ Transfer Manager │              │ Transfer Manager │
└──────────────────┘              └──────────────────┘
```

不允许设计中心服务器。

---

# 7. 默认端口

默认：

```text
UDP Discovery
39520

TCP / HTTPS Transfer
39521
```

端口必须支持在设置中修改。

例如：

```text
UDP: 39520

TCP: 39521
```

如果端口被占用：

软件不得直接崩溃。

需要：

1. 检测端口占用
2. 显示占用情况
3. 允许修改端口
4. 重新启动网络服务

---

# 8. 项目目录结构

Solution：

```text
LanTransfer.sln
```

项目结构：

```text
LanTransfer
│
├── src
│   │
│   ├── LanTransfer.App
│   │   ├── Views
│   │   ├── ViewModels
│   │   ├── Controls
│   │   ├── Converters
│   │   └── Resources
│   │
│   ├── LanTransfer.Core
│   │   ├── Transfers
│   │   ├── Devices
│   │   ├── Files
│   │   ├── Hashing
│   │   └── Interfaces
│   │
│   ├── LanTransfer.Network
│   │   ├── Discovery
│   │   ├── Client
│   │   ├── Server
│   │   └── Protocol
│   │
│   ├── LanTransfer.Security
│   │   ├── Certificates
│   │   ├── Pairing
│   │   └── Trust
│   │
│   ├── LanTransfer.Storage
│   │   ├── Database
│   │   ├── Repositories
│   │   └── Entities
│   │
│   └── LanTransfer.Common
│       ├── Models
│       ├── Constants
│       ├── Extensions
│       └── Logging
│
├── tests
│   ├── LanTransfer.Core.Tests
│   ├── LanTransfer.Network.Tests
│   └── LanTransfer.IntegrationTests
│
├── docs
│   ├── architecture.md
│   ├── protocol.md
│   ├── api.md
│   ├── database.md
│   └── testing.md
│
└── README.md
```

严禁把全部功能写入一个 WPF 项目。

UI、业务逻辑、网络层、存储层必须解耦。

---

# 9. 核心模块

必须实现以下模块。

## 9.1 DiscoveryService

负责局域网设备发现。

功能：

```text
UDP广播
设备响应
设备缓存
在线状态
离线检测
```

---

## 9.2 DeviceManager

维护：

```text
DeviceId
DeviceName
IP
Port
AppVersion
LastSeen
TrustState
OnlineState
```

---

## 9.3 TransferManager

负责统一管理：

```text
创建传输
开始传输
暂停
继续
取消
重试
完成
失败
```

UI 不得直接负责文件传输逻辑。

---

## 9.4 ChunkManager

负责：

```text
文件分块
Chunk编号
Chunk位置
Chunk长度
已完成Chunk记录
断点续传
```

默认 Chunk：

```text
4 MiB
```

配置允许：

```text
1 MiB
2 MiB
4 MiB
8 MiB
16 MiB
```

默认：

```text
4 MiB
```

---

## 9.5 HashManager

负责：

```text
SHA-256
```

用途：

* 文件发送前摘要
* 文件接收后完整性校验
* 可选 Chunk 校验

必须使用流式 Hash。

禁止一次性载入整个文件。

---

## 9.6 TransferServer

Kestrel 服务。

负责：

```text
设备信息
配对
建立传输
文件接收
Chunk接收
状态查询
暂停
取消
完整性校验
```

---

## 9.7 TransferClient

负责：

```text
连接目标设备
发送文件描述
上传Chunk
状态查询
断点续传
取消任务
```

---

# 10. 局域网设备发现协议

使用：

```text
UDP Broadcast
```

端口：

```text
39520
```

广播地址：

```text
255.255.255.255
```

同时应考虑多网卡环境。

不得只取第一张网卡。

过滤：

```text
Loopback
虚拟无效接口
未启用接口
```

---

# 11. Discovery 数据格式

使用 JSON。

发现请求：

```json
{
  "protocol": "lan-transfer",
  "type": "discover",
  "version": 1,
  "deviceId": "UUID",
  "deviceName": "TEST-PC01",
  "appVersion": "1.0.0",
  "port": 39521
}
```

发现响应：

```json
{
  "protocol": "lan-transfer",
  "type": "discover-response",
  "version": 1,
  "deviceId": "UUID",
  "deviceName": "TEST-PC02",
  "appVersion": "1.0.0",
  "port": 39521
}
```

IP 不以 JSON 中声明值为准。

应优先采用：

**UDP 数据包实际 Source IP。**

防止错误 IP 或伪造地址影响连接。

---

# 12. DeviceId

第一次启动时生成：

```text
UUID v4
```

保存到本地配置。

以后启动不得重新生成。

DeviceId 用于唯一标识设备。

不能使用：

```text
IP
MAC
计算机名称
```

作为唯一 ID。

因为这些数据可能变化。

---

# 13. 在线状态

每个设备维护：

```text
LastSeen
```

建议广播间隔：

```text
3 秒
```

超过：

```text
10 秒
```

没有收到设备信息：

```text
Online → Offline
```

具体值做成常量。

---

# 14. 手工连接

即使 UDP 广播失效，也必须能够手工连接。

界面：

```text
IP地址：

[192.168.1.102        ]

端口：

[39521]

[连接]
```

应用场景：

* UDP 广播被禁止
* VLAN 环境
* 多网卡
* 企业网络
* VPN 干扰

---

# 15. 网络诊断功能

必须设计独立：

**网络诊断**

页面。

至少显示：

```text
本机设备名称
DeviceId

IPv4地址
子网掩码

UDP发现端口
TCP传输端口

Server运行状态

端口监听状态

已发现设备
```

提供：

```text
测试目标连接
```

用户输入：

```text
IP
Port
```

测试：

```text
DNS（如果输入主机名）
TCP Connect
HTTP/HTTPS API
协议版本
```

返回类似：

```text
192.168.1.102

TCP连接：成功
服务响应：成功
协议版本：1
延迟：3 ms
```

或者：

```text
TCP连接失败

可能原因：

目标软件未启动
Windows 防火墙阻止
端口配置不同
网络禁止终端互访
```

不得尝试绕过网络安全策略。

---

# 16. 文件传输流程

完整流程：

```text
用户选择设备
      ↓
选择文件
      ↓
扫描文件
      ↓
建立 Transfer
      ↓
目标电脑提示接收
      ↓
用户允许
      ↓
开始传输
      ↓
Chunk上传
      ↓
写入 .part 文件
      ↓
全部Chunk完成
      ↓
SHA-256校验
      ↓
成功
      ↓
.part → 原文件名
```

---

# 17. 创建传输 API

示例：

```http
POST /api/v1/transfers
```

Request：

```json
{
  "transferType": "file",
  "fileName": "firmware.zip",
  "relativePath": "",
  "fileSize": 4294967296,
  "sha256": "HEX_STRING",
  "chunkSize": 4194304
}
```

Response：

```json
{
  "transferId": "UUID",
  "state": "waiting-approval"
}
```

---

# 18. 接收确认

默认：

陌生设备发送文件必须由用户确认。

目标电脑弹窗：

```text
PC-A 想向你发送文件

文件：
firmware.zip

大小：
4.00 GB

[接收]

[拒绝]
```

可信设备可以在设置中启用：

```text
自动接收可信设备文件
```

默认：

**关闭。**

---

# 19. Chunk 上传接口

```http
PUT /api/v1/transfers/{transferId}/chunks/{chunkIndex}
```

HTTP Body：

```text
application/octet-stream
```

每次只上传一个 Chunk。

服务端必须支持：

```text
异步 Stream
```

严禁：

```text
ReadAllBytes
```

处理大文件。

---

# 20. 文件写入方式

目标文件首先写入：

```text
filename.ext.part
```

只有：

```text
所有Chunk完成
+
SHA-256校验成功
```

之后才执行：

```text
filename.ext.part
→
filename.ext
```

因此传输中断时不得出现一个看似正常的损坏文件。

---

# 21. 断点续传

核心要求。

数据库必须记录：

```text
TransferId
FilePath
FileSize
ChunkSize
TotalChunks
CompletedChunks
SHA256
State
```

状态查询：

```http
GET /api/v1/transfers/{transferId}
```

示例：

```json
{
  "transferId": "UUID",
  "state": "transferring",
  "totalChunks": 1024,
  "completedChunks": [
    0,
    1,
    2,
    3,
    4
  ]
}
```

断线重连后：

发送端读取：

```text
completedChunks
```

仅发送缺失 Chunk。

不能从 0 重新发送整个文件。

---

# 22. Chunk 状态存储

不要把几万个 Chunk 全部保存为巨大 JSON 字符串。

应设计合理的数据结构，例如：

```text
Transfer
+
TransferChunk
```

数据库：

```text
Transfers

TransferChunks
```

或者使用：

```text
Bitmap
```

持久化。

Agent 自行选择性能较好的实现，但需要在：

```text
docs/database.md
```

说明方案。

---

# 23. 文件夹传输

用户选择文件夹：

```text
D:\Project
```

需要保留目录结构：

```text
Project
├── docs
│   └── readme.md
├── firmware
│   └── app.bin
└── image.png
```

接收后：

```text
Project
├── docs
│   └── readme.md
├── firmware
│   └── app.bin
└── image.png
```

不能将全部文件平铺。

---

# 24. 文件路径安全

必须防止：

```text
../
..\ 
绝对路径
路径穿越
```

远程发送端不得通过伪造：

```text
../../Windows/System32/
```

写入任意目录。

接收端所有文件必须限制在用户指定：

```text
DownloadRoot
```

目录下面。

必须使用安全路径规范化检查。

---

# 25. 文件重名处理

如果目标已有：

```text
test.zip
```

UI 提示：

```text
目标文件已存在

○ 覆盖
○ 重命名
○ 跳过
```

默认：

```text
重命名
```

自动：

```text
test (1).zip
test (2).zip
```

不得静默覆盖。

---

# 26. 文件完整性

发送前：

```text
SHA-256
```

接收完成：

重新计算：

```text
SHA-256
```

比较：

```text
SenderHash == ReceiverHash
```

一致：

```text
Completed
```

不一致：

```text
VerificationFailed
```

文件不得直接标记成功。

---

# 27. 暂停功能

用户点击：

```text
暂停
```

要求：

当前正在上传 Chunk 可以完成。

之后停止发送新 Chunk。

状态：

```text
Paused
```

再次点击：

```text
继续
```

恢复。

---

# 28. 取消功能

点击：

```text
取消
```

确认：

```text
确定取消传输？

[取消传输]
[继续传输]
```

取消后：

默认保留：

```text
.part
+
resume metadata
```

允许后续重新开始。

另外提供：

```text
删除未完成文件
```

---

# 29. 传输状态机

统一状态：

```text
Pending
WaitingApproval
Preparing
Transferring
Paused
Verifying
Completed
Rejected
Cancelled
Failed
VerificationFailed
```

禁止 UI 自行定义另一套状态。

所有模块统一使用同一个：

```text
TransferState
```

Enum。

---

# 30. 速度计算

界面显示：

```text
MB/s
```

不要直接使用：

```text
总字节 / 总时间
```

作为实时速度。

建议使用：

```text
最近 2～5 秒滑动平均
```

避免数字剧烈跳动。

同时计算：

```text
ETA
```

即预计剩余时间。

---

# 31. UI 首页

设计参考：

```text
┌───────────────────────────────────────────────┐
│ LAN Transfer                           ─ □ ×  │
├───────────────────────────────────────────────┤
│ 本机：TEST-PC01             192.168.1.101     │
│                                               │
│ 附近设备                                      │
│                                               │
│ ● TEST-PC02                     在线          │
│   192.168.1.102                               │
│                                               │
│ ● NOTEBOOK                      在线          │
│   192.168.1.105                               │
│                                               │
│ [添加IP设备]                                  │
│                                               │
├───────────────────────────────────────────────┤
│ [发送文件]      [发送文件夹]                  │
│                                               │
│ 也可以将文件拖到这里                          │
└───────────────────────────────────────────────┘
```

---

# 32. 设备详情

点击设备：

```text
TEST-PC02

192.168.1.102

状态：
在线

可信设备：
是

版本：
1.0.0

[发送文件]

[发送文件夹]

[解除信任]
```

---

# 33. 传输页面

示例：

```text
firmware.zip

TEST-PC01
↓
TEST-PC02

████████████████░░░░░░

72%

2.88 GB / 4.00 GB

速度：
93.6 MB/s

预计剩余：
12 秒

[暂停]

[取消]
```

---

# 34. 传输任务列表

同时可以存在多个 Transfer。

例如：

```text
firmware.zip
72%
93 MB/s

video.mp4
等待

project
已完成
```

第一版可以限制：

```text
同时进行 1 个传输任务
```

其他任务：

```text
Queued
```

以后再增加并发传输。

---

# 35. 拖放

主页支持：

```text
Drag & Drop
```

将：

```text
文件
文件夹
多文件
```

拖入窗口。

如果当前选择了设备：

直接进入发送确认。

如果没有选择设备：

弹出设备选择窗口。

---

# 36. 安全设计

程序第一次运行生成：

```text
Device Identity
+
TLS Certificate
```

通信优先使用：

```text
HTTPS
```

不能因为使用局域网就默认认为网络可信。

---

# 37. 设备配对

第一次连接陌生设备时：

两端显示相同：

```text
安全验证码

482917
```

页面：

```text
确认两台电脑显示的验证码相同

482917

[确认]

[取消]
```

确认成功：

```text
Trusted Device
```

保存：

```text
DeviceId
CertificateFingerprint
DeviceName
FirstSeen
LastSeen
```

以后连接：

校验证书指纹。

如果某个已信任 DeviceId 的证书发生变化：

必须提示：

```text
设备身份发生变化

请重新确认设备
```

不得静默接受。

---

# 38. HTTPS 证书

应用自行生成自签名证书。

不需要：

```text
安装到 Windows 根证书存储
```

应用自己维护信任。

证书必须包含：

```text
Public Key
Private Key
Fingerprint
```

Private Key 只能保存本机。

建议使用 Windows 数据保护机制保护本地敏感数据。

---

# 39. 配置

配置项：

```text
DeviceName

DiscoveryPort

TransferPort

DownloadPath

ChunkSize

AutoStart

AutoAcceptTrustedDevice

Notifications

Theme

LogLevel
```

默认下载目录建议：

```text
Downloads\LAN Transfer
```

---

# 40. SQLite

至少设计：

```text
Devices

Transfers

TransferFiles

TransferChunks

Settings
```

Devices：

```text
DeviceId
DeviceName
CertificateFingerprint
TrustState
FirstSeen
LastSeen
```

Transfers：

```text
TransferId
Direction
RemoteDeviceId
State
TotalSize
TransferredSize
CreatedAt
CompletedAt
```

TransferFiles：

```text
FileId
TransferId
RelativePath
FileName
FileSize
SHA256
ChunkSize
TotalChunks
State
```

---

# 41. Windows 防火墙

程序不能默认关闭 Windows 防火墙。

严禁：

```text
netsh firewall disable
```

如果端口被阻止：

显示：

```text
Windows 防火墙可能阻止了 LAN Transfer

请允许 LAN Transfer 在专用网络通信。
```

如需自动创建防火墙规则：

必须：

1. 用户主动执行
2. 明确显示行为
3. 只开放应用使用端口
4. 不允许关闭整个防火墙

---

# 42. 网络类型

优先允许：

```text
Private Network
```

对于：

```text
Public Network
```

显示安全提醒。

不要默认在公共网络自动接受传输。

---

# 43. 多网卡

必须考虑：

```text
Ethernet
Wi-Fi
VPN
VMware
Hyper-V
WSL
Docker
VirtualBox
```

设备发现时不要简单：

```text
Dns.GetHostEntry().AddressList[0]
```

应枚举有效 NetworkInterface。

至少正确识别：

```text
Up
IPv4
非 Loopback
```

---

# 44. IPv4

V1 只要求：

```text
IPv4
```

IPv6 暂不实现。

协议设计不要硬编码到无法扩展 IPv6。

---

# 45. API

至少实现：

```text
GET
/api/v1/device

GET
/api/v1/health

POST
/api/v1/pair

POST
/api/v1/transfers

GET
/api/v1/transfers/{id}

POST
/api/v1/transfers/{id}/approve

POST
/api/v1/transfers/{id}/reject

PUT
/api/v1/transfers/{id}/chunks/{chunk}

POST
/api/v1/transfers/{id}/pause

POST
/api/v1/transfers/{id}/resume

POST
/api/v1/transfers/{id}/cancel

POST
/api/v1/transfers/{id}/complete
```

具体 DTO 必须记录：

```text
docs/api.md
```

---

# 46. API 版本

所有 API 使用：

```text
/api/v1/
```

协议必须带：

```text
ProtocolVersion
```

未来软件升级不能导致旧客户端直接崩溃。

如果协议不兼容：

显示：

```text
目标设备 LAN Transfer 版本不兼容
```

---

# 47. 错误模型

统一返回：

```json
{
  "success": false,
  "errorCode": "TRANSFER_NOT_FOUND",
  "message": "Transfer not found"
}
```

禁止每个 API 使用完全不同的错误结构。

---

# 48. 日志

日志必须记录：

```text
启动
关闭
Server启动
端口
设备发现
设备上线
设备离线
连接
配对
创建Transfer
开始传输
暂停
恢复
取消
异常
Hash失败
传输完成
```

不得记录：

```text
私钥
完整证书私钥
敏感Token
文件内容
```

日志位置：

```text
%LOCALAPPDATA%\LanTransfer\Logs
```

日志滚动：

```text
Daily
```

限制历史日志数量。

---

# 49. 异常处理

必须处理：

```text
拔网线
Wi-Fi掉线
目标程序关闭
目标电脑关机
端口改变
文件被删除
文件被其他程序锁定
磁盘空间不足
接收目录无权限
文件名非法
文件重名
Hash错误
数据库损坏
发送过程中IP改变
```

应用不能因为单个传输失败而退出。

---

# 50. 磁盘空间检查

接收前检查：

```text
AvailableSpace
```

如果文件：

```text
20 GB
```

目标磁盘仅剩：

```text
10 GB
```

必须拒绝或警告。

返回：

```text
INSUFFICIENT_DISK_SPACE
```

---

# 51. 长路径

需要处理 Windows 长路径问题。

内部尽量使用：

```text
Path
```

相关标准 API。

不得人为限制：

```text
260字符
```

但对于无法创建的路径必须明确反馈。

---

# 52. 文件变化检测

发送过程中如果源文件：

```text
长度发生变化
修改时间发生变化
```

应终止或提示：

```text
源文件在传输过程中发生变化
```

不能继续认为 Hash 有效。

---

# 53. UI 线程

所有：

```text
网络操作
文件IO
Hash
SQLite
```

不能阻塞 WPF UI Thread。

全部使用：

```text
async/await
```

确保：

```text
拖窗口
点击按钮
进度动画
```

不卡死。

---

# 54. CancellationToken

所有长任务必须支持：

```text
CancellationToken
```

包括：

```text
Hash
文件读取
HTTP请求
Chunk上传
等待
```

---

# 55. 内存目标

传输 100GB 文件时：

应用内存不应随着文件大小线性增加。

目标：

普通文件传输时：

```text
< 300 MB
```

更理想：

```text
< 150 MB
```

禁止构建：

```text
100GB MemoryStream
```

之类实现。

---

# 56. 性能目标

千兆局域网：

目标：

```text
≥ 80 MB/s
```

理想：

```text
90 ～ 115 MB/s
```

具体受：

```text
网卡
交换机
SSD
CPU
杀毒软件
```

影响。

软件自身不得产生明显性能瓶颈。

---

# 57. 性能优化原则

第一版：

```text
单文件
单HTTP上传流
Chunk顺序上传
```

先保证：

```text
稳定性
正确性
断点续传
```

不要一开始开发复杂：

```text
32线程并发Chunk
```

后续根据性能测试再增加：

```text
2～4 Chunk并发
```

---

# 58. 单元测试

至少测试：

```text
Chunk计算
SHA-256
路径规范化
文件重名
状态机
Transfer恢复
DeviceId
协议序列化
数据库Repository
```

---

# 59. 集成测试

创建 Integration Test：

自动在 localhost 启动：

```text
Server
+
Client
```

测试：

### Test 01

```text
1KB文件
```

### Test 02

```text
10MB文件
```

### Test 03

```text
500MB文件
```

大文件测试可以：

```text
Explicit
```

避免每次 CI 执行。

---

# 60. 必须验证的断点测试

例如：

发送：

```text
1GB.bin
```

传输至：

```text
40%
```

模拟：

```text
断网
```

重新连接。

要求：

```text
从40%左右继续
```

而不是：

```text
0%
```

重新开始。

---

# 61. 完整性测试

准备：

```text
test.bin
```

发送端：

```text
SHA256=A
```

接收完成：

```text
SHA256=B
```

必须：

```text
A == B
```

人为修改 `.part`：

最终校验必须失败。

---

# 62. UI 验收

启动后：

3 秒左右应看到同局域网另一台运行 LAN Transfer 的电脑。

用户完成最基本操作：

```text
打开软件
↓
选择设备
↓
选择文件
↓
发送
```

不应该要求用户理解：

```text
SMB
HTTP
TCP
UDP
端口
子网
```

高级信息放入：

```text
设置
网络诊断
```

---

# 63. 安装方式

优先同时支持：

## Portable

例如：

```text
LanTransfer.exe
```

或一个便携目录。

## Installer

后续可提供：

```text
MSIX
或
安装程序
```

第一阶段以：

```text
Portable x64
```

为主。

---

# 64. 发布模式

使用：

```text
Release
win-x64
```

考虑：

```text
Self-contained
```

使目标电脑不需要预装 .NET Runtime。

最终输出：

```text
dist\
```

Agent 必须提供：

```text
publish.ps1
```

用于一键生成 Release。

---

# 65. 第一阶段开发顺序

严格按照以下顺序推进。

## Phase 0：项目骨架

完成：

```text
Solution
项目拆分
DI
Logging
Configuration
WPF主窗口
```

验收：

应用可以启动。

---

## Phase 1：网络验证

完成：

```text
Kestrel
/api/v1/health
/api/v1/device
手工IP连接
网络诊断
```

这是第一优先级。

先验证：

**两台目标 Windows PC 是否真的允许自定义 TCP 通信。**

这一阶段完成后必须能够：

```text
PC-A
→
PC-B:39521
```

成功访问。

如果这一步失败：

先排查网络策略，不继续堆文件传输代码。

---

## Phase 2：UDP发现

实现：

```text
UDP Broadcast
设备列表
Online
Offline
```

验收：

两台客户端自动看到对方。

---

## Phase 3：基础文件传输

只实现：

```text
单文件
```

先不实现断点。

目标：

```text
A → B
```

成功传文件。

并验证 SHA-256。

---

## Phase 4：Chunk

将文件改为：

```text
4 MiB Chunk
```

实现 Chunk 状态。

---

## Phase 5：断点续传

实现：

```text
断网
恢复
查询Chunk
继续
```

---

## Phase 6：文件夹

实现：

```text
目录扫描
relativePath
目录恢复
```

---

## Phase 7：安全

实现：

```text
TLS
证书
设备配对
可信设备
证书指纹
```

在安全模块未完成前，仅允许开发测试使用，不作为正式发布版本。

---

## Phase 8：UI优化

完成：

```text
拖放
传输卡片
速度
ETA
通知
历史记录
```

---

## Phase 9：发布

完成：

```text
Portable
Self-contained
publish.ps1
README
```

---

# 66. MVP 定义

第一版正式可用至少必须拥有：

```text
✓ 手工IP连接

✓ UDP设备发现

✓ 文件发送

✓ 文件夹发送

✓ 多文件

✓ 接收确认

✓ 4MiB分块

✓ 断点续传

✓ SHA-256

✓ 实时进度

✓ 实时速度

✓ ETA

✓ 暂停

✓ 继续

✓ 取消

✓ 网络诊断

✓ HTTPS

✓ 设备可信配对

✓ SQLite历史记录

✓ Windows 10/11 x64
```

---

# 67. 暂不实现

V1 不开发：

```text
公网传输

NAT穿透

云服务器

账号系统

Web账号登录

好友系统

聊天

剪贴板同步

屏幕控制

远程桌面

远程Shell

远程命令执行

手机端

互联网中继

文件同步盘
```

严禁 Scope Creep。

---

# 68. 非目标

软件不是：

```text
远程管理工具

远控软件

VPN

代理

隧道软件

网络策略绕过工具
```

只负责：

**经过用户确认的局域网文件传输。**

---

# 69. Agent 编码规则

Agent 开发过程中必须遵守：

1. 禁止创建一个超大类完成全部功能。
2. 单一职责。
3. 网络层不得依赖 UI。
4. Core 不得依赖 WPF。
5. 所有公共接口使用明确模型。
6. 所有 IO 使用异步接口。
7. 长任务支持 CancellationToken。
8. 不允许吞异常。
9. 异常必须记录日志。
10. 不允许硬编码用户目录。
11. 不允许硬编码目标 IP。
12. 端口必须可配置。
13. 安全相关数据不得打印到日志。
14. 文件路径必须进行安全校验。
15. 每完成一个模块必须编译。
16. 不允许一次修改几十个模块后才运行。
17. 每个 Phase 完成后运行测试。
18. 新增协议必须同步更新 docs。
19. 不得为了“跑通”关闭 TLS/路径检查等安全机制。
20. 不得尝试绕过系统或网络管理员配置的安全策略。

---

# 70. Agent 工作方式

每个 Phase 使用：

```text
分析
↓
设计
↓
实现
↓
编译
↓
测试
↓
修复
↓
更新文档
↓
再进入下一阶段
```

禁止：

```text
一次性生成整个项目但从不编译
```

---

# 71. Agent 首次任务

首先只执行：

```text
Phase 0
+
Phase 1
```

不要立即开发完整文件传输系统。

第一目标是确认基础网络架构可行。

Agent 第一次交付必须包含：

```text
LanTransfer.sln

WPF应用

Kestrel Server

GET /api/v1/health

GET /api/v1/device

手工输入目标IP

TCP/API连接检测

网络诊断页面

Serilog

配置系统

README
```

然后进行双机测试。

---

# 72. 第一阶段双机验收

电脑 A：

```text
192.168.1.101
```

电脑 B：

```text
192.168.1.102
```

两台运行：

```text
LanTransfer
```

B：

```text
TCP 39521 Listening
```

A 输入：

```text
192.168.1.102
```

点击：

```text
测试连接
```

应显示：

```text
连接成功

设备：
PC-B

IP：
192.168.1.102

端口：
39521

协议：
1

延迟：
xx ms
```

B 同样应可以连接 A。

只有此测试完成：

才进入：

```text
Phase 2
```

---

# 73. 最终验收场景

准备：

```text
PC-A
PC-B
```

同一 LAN。

测试以下场景。

### 场景 1

发送：

```text
1MB
```

通过。

### 场景 2

发送：

```text
1GB
```

通过。

### 场景 3

发送：

```text
20GB
```

通过。

### 场景 4

发送包含：

```text
1000 个小文件
```

的文件夹。

目录结构完全一致。

### 场景 5

传输至 50%：

拔网线。

重新连接。

从断点继续。

### 场景 6

目标磁盘空间不足。

软件正确阻止。

### 场景 7

发送过程中关闭接收端。

发送端不崩溃。

### 场景 8

重新启动接收端。

恢复传输。

### 场景 9

修改 `.part` 数据。

SHA-256 检测失败。

### 场景 10

陌生设备发送。

必须提示确认。

### 场景 11

可信设备证书发生变化。

必须重新确认身份。

### 场景 12

UDP 被禁止。

手工 IP 仍可以传输。

---

# 74. 最终交付物

Agent 最终必须提供：

```text
完整源代码

LanTransfer.sln

README.md

architecture.md

protocol.md

api.md

database.md

testing.md

publish.ps1

Release Portable Build
```

README 必须包含：

```text
项目简介

开发环境

编译方法

运行方法

双机测试方法

默认端口

Windows防火墙说明

网络诊断方法

常见问题
```

---

# 75. 完成标准

项目只有满足以下条件才算完成：

```text
两台 Windows PC

无需 SMB

无需 Windows 文件共享

无需互联网

无需中心服务器

能够：

发现设备
↓
完成设备确认
↓
发送文件/文件夹
↓
显示实时进度
↓
断网后续传
↓
SHA-256校验
↓
成功保存
```

并且：

```text
20GB以上文件传输不发生明显内存增长

断线不会导致程序崩溃

文件失败不会被误标记成功

陌生设备不能静默写入文件

远程提供的路径不能逃逸接收目录

日志能够定位常见网络和传输问题
```

---

# 76. 给开发 Agent 的最终指令

你现在是本项目的高级 Windows/C# 软件工程师。

请严格按照本文档开发 LAN Transfer。

不要一次性实现全部功能。

首先完成：

```text
Phase 0
Phase 1
```

重点验证：

```text
WPF程序
+
Kestrel服务器
+
手工IP连接
+
两台Windows电脑TCP/HTTPS互通
+
网络诊断
```

在基础网络通信通过之前，不开始实现复杂的文件分块和断点续传。

每完成一个阶段：

```text
dotnet restore
dotnet build
dotnet test
```

确保无编译错误后再进入下一阶段。

所有架构、API、协议和数据库变更同步写入：

```text
/docs
```

遇到普通技术细节时自行做合理工程决策，不要因为小问题停止开发并询问用户。

只有涉及以下重大改变时才需要重新确认：

```text
修改核心技术栈
修改网络协议
引入中心服务器
引入互联网服务
取消断点续传
取消TLS
取消设备身份验证
改变Windows-only目标
```
# LAN Transfer + 双向同步开发任务书

> 本文在原 LAN Transfer 文件传输功能基础上增加“双向文件夹同步”能力。
> 原有单次文件传输功能继续保留，不得被同步功能替代。

---

# 77. 新增核心功能：双向同步

软件除普通文件传输外，必须支持：

```text
PC-A 文件夹
        ⇅
PC-B 文件夹
```

例如：

```text
PC-A
D:\Project

⇅

PC-B
E:\Project
```

任意一台电脑：

```text
新增文件
修改文件
删除文件
重命名文件
移动文件
创建目录
删除目录
```

另一台电脑均能够自动同步。

目标体验：

```text
PC-A 修改文件
      ↓
检测变化
      ↓
通知 PC-B
      ↓
PC-B 获取文件
      ↓
两端最终一致
```

---

# 78. 同步不是简单“互相复制”

严禁实现成：

```text
A 扫描一次 → 复制给 B
B 扫描一次 → 复制给 A
```

否则极容易造成：

```text
死循环
重复传输
旧文件覆盖新文件
删除文件重新出现
冲突文件丢失
```

必须设计独立的：

```text
Sync Engine
```

---

# 79. 同步模式

V1 至少支持：

```text
双向同步
Two-Way Sync
```

后续可扩展：

```text
A → B 单向同步
B → A 单向同步
```

但 V1 核心目标：

```text
A ⇄ B
```

---

# 80. 同步任务 SyncPair

用户创建一个同步关系。

例如：

```text
SyncPair

名称：
研发资料

PC-A：
D:\Development

PC-B：
E:\Development
```

每个同步任务拥有：

```text
SyncPairId
```

使用 UUID。

---

# 81. SyncPair 数据模型

至少包含：

```text
SyncPairId

Name

LocalPath

RemoteDeviceId

RemotePath

Mode

Enabled

CreatedAt

LastSyncAt

Status
```

Mode：

```text
TwoWay
SendOnly
ReceiveOnly
```

V1 UI 可只暴露：

```text
TwoWay
```

但底层模型预留其他模式。

---

# 82. 同步任务创建流程

用户：

```text
选择远程设备
      ↓
选择本地文件夹
      ↓
选择远程文件夹
      ↓
创建同步关系
      ↓
首次扫描
      ↓
建立同步基线
      ↓
开始实时同步
```

---

# 83. 同步关系必须双端确认

例如 PC-A 发起：

```text
同步请求

A：
D:\Project

B：
E:\Project
```

PC-B 必须弹出：

```text
PC-A 请求创建同步文件夹

同步名称：
Project

远程设备：
PC-A

本机目录：
E:\Project

模式：
双向同步

[允许]

[拒绝]
```

未经授权不得自动创建长期同步关系。

---

# 84. 同步页面

主界面新增：

```text
文件传输
同步任务
设备
历史记录
设置
```

同步页面示例：

```text
┌─────────────────────────────────────┐
│ 同步任务                            │
│                                     │
│ ● 项目资料                          │
│   PC-A ⇄ PC-B                       │
│                                     │
│   D:\Project                        │
│   ⇅                                 │
│   E:\Project                        │
│                                     │
│   状态：已同步                      │
│   最后同步：15:42:31                │
│                                     │
│   [立即同步] [暂停] [设置]          │
└─────────────────────────────────────┘
```

---

# 85. Sync Engine

新增：

```text
LanTransfer.Sync
```

建议目录：

```text
LanTransfer.Sync
│
├── Engine
│   └── SyncEngine
│
├── Scanner
│   └── DirectoryScanner
│
├── Watcher
│   └── FileChangeWatcher
│
├── Planner
│   └── SyncPlanner
│
├── Conflict
│   └── ConflictResolver
│
├── Metadata
│   └── SyncMetadataManager
│
└── Models
```

---

# 86. 核心同步架构

同步流程：

```text
文件系统
   ↓
Change Detector
   ↓
Sync Metadata
   ↓
Sync Planner
   ↓
Conflict Detector
   ↓
Transfer Manager
   ↓
Remote Device
```

禁止：

```text
FileSystemWatcher
        ↓
直接发送文件
```

中间必须经过：

```text
Sync Planner
```

---

# 87. 文件变化检测

实时检测使用：

```text
FileSystemWatcher
```

监控：

```text
Created
Changed
Deleted
Renamed
```

但：

**不能只依赖 FileSystemWatcher。**

原因包括：

```text
事件可能丢失
事件可能重复
大量文件操作导致缓冲区溢出
程序关闭期间变化无法捕获
```

因此必须：

```text
FileSystemWatcher
+
定期完整扫描
```

---

# 88. 完整扫描

每次软件启动后：

```text
扫描同步文件夹
```

并与：

```text
同步数据库
```

比较。

另外周期性扫描。

默认：

```text
每 10 分钟
```

可以配置：

```text
5分钟
10分钟
30分钟
60分钟
```

实时变化仍由 FileSystemWatcher 负责。

---

# 89. 文件同步元数据

每个同步文件必须保存：

```text
SyncPairId

RelativePath

FileId

FileSize

LastWriteTimeUtc

SHA256

LocalVersion

RemoteVersion

State

Deleted

LastSyncedAt
```

---

# 90. RelativePath 是核心标识

例如：

```text
D:\Project\docs\a.txt
```

同步数据库中只记录：

```text
docs\a.txt
```

另外一端：

```text
E:\Project\docs\a.txt
```

两端通过：

```text
RelativePath
```

建立对应关系。

不能保存另一端的完整路径作为文件身份。

---

# 91. FileVersion

每次本地文件发生实际内容变化：

```text
Version++
```

例如：

```text
a.txt

Version 1
Version 2
Version 3
```

避免单独依赖：

```text
LastWriteTime
```

判断新旧。

---

# 92. 为什么不能只比较修改时间

禁止使用：

```text
谁 LastWriteTime 更新
谁覆盖另一边
```

作为唯一规则。

原因：

```text
PC-A 时间：15:00:05
PC-B 时间：15:00:30
```

两台电脑系统时间可能存在偏差。

因此修改时间只能作为：

```text
快速变化检测
```

不能作为唯一冲突判断依据。

---

# 93. 文件指纹

建议两级检测。

第一级快速检查：

```text
FileSize
LastWriteTimeUtc
```

如果没有变化：

无需重新 Hash。

如果疑似变化：

```text
SHA-256
```

最终确认。

这样避免不停计算大文件 SHA-256。

---

# 94. 同步基线

创建 SyncPair 时必须建立：

```text
Baseline
```

基线代表：

```text
双方最后一次确认一致的状态
```

例如：

```text
Baseline

a.txt
Hash = ABC123
Version = 3
```

随后：

PC-A：

```text
Hash DEF456
```

PC-B：

```text
ABC123
```

说明：

```text
只有 A 修改
```

因此：

```text
A → B
```

---

# 95. 双向同步核心判断

必须比较：

```text
Baseline
Local
Remote
```

不能只比较：

```text
Local
Remote
```

核心规则：

### 情况 1

```text
Local == Baseline
Remote == Baseline
```

动作：

```text
无需同步
```

---

### 情况 2

```text
Local != Baseline
Remote == Baseline
```

说明：

```text
本地修改
```

动作：

```text
Local → Remote
```

---

### 情况 3

```text
Local == Baseline
Remote != Baseline
```

说明：

```text
远程修改
```

动作：

```text
Remote → Local
```

---

### 情况 4

```text
Local != Baseline
Remote != Baseline
```

如果：

```text
Local == Remote
```

说明双方修改结果恰好一致。

动作：

```text
更新 Baseline
```

如果：

```text
Local != Remote
```

则：

```text
Conflict
```

---

# 96. 冲突定义

最典型情况：

PC-A 修改：

```text
report.docx
```

PC-B 同时也修改：

```text
report.docx
```

且都基于：

```text
Version 5
```

则不能：

```text
A覆盖B
```

也不能：

```text
B覆盖A
```

必须进入：

```text
Conflict
```

---

# 97. 默认冲突策略

默认：

**保留双方文件。**

禁止默认覆盖任意一边。

例如：

原文件：

```text
report.docx
```

保留当前设备版本：

```text
report.docx
```

另一设备版本保存为：

```text
report (conflict-PC-B-20260924-154501).docx
```

并在 UI 中：

```text
⚠ 同步冲突
```

---

# 98. 冲突处理页面

用户可选择：

```text
使用本机版本

使用远程版本

保留两个版本
```

文本文件未来可增加：

```text
Diff / Merge
```

但 V1 不需要做文本合并器。

---

# 99. 删除同步

删除不能简单理解为：

```text
文件不存在
```

因为：

PC-A 删除：

```text
a.txt
```

如果 PC-B 仍存在：

错误算法可能会：

```text
PC-B → PC-A
```

导致文件重新出现。

因此必须使用：

```text
Tombstone
```

即“删除墓碑”。

---

# 100. Tombstone

删除文件后数据库记录：

```text
RelativePath:
docs\a.txt

Deleted:
true

DeletedAt:
...

Version:
4
```

而不是直接删除数据库记录。

这样另一端知道：

```text
这个文件不是缺失

而是被用户明确删除
```

---

# 101. 删除传播

例如：

基线：

```text
a.txt Version 3
```

PC-A：

```text
Deleted Version 4
```

PC-B：

```text
Version 3
```

判断：

```text
A发生删除
B未修改
```

动作：

```text
删除 B 的 a.txt
```

然后：

```text
Baseline = Deleted Version 4
```

---

# 102. 删除冲突

例如：

基线：

```text
a.txt
```

之后：

PC-A：

```text
删除 a.txt
```

PC-B：

```text
修改 a.txt
```

此时属于：

```text
Delete / Modify Conflict
```

不能直接删除 PC-B 文件。

默认：

```text
保留 PC-B 修改后的文件
+
产生冲突记录
```

让用户决定。

---

# 103. Tombstone 保留时间

删除记录不能永久无限增长。

默认：

```text
30 天
```

可配置：

```text
7天
30天
90天
```

只有确认：

```text
双方均已收到删除状态
```

并超过保留期后：

才能清理 Tombstone。

---

# 104. 文件重命名

例如：

```text
old.txt
↓
new
```


其余情况按照本任务书持续实现。
